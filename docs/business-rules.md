> ⚡ Auto-generated by sdd_reverse_engineer — review and refine as needed

# go-course — Business Rules

> Generated by [SDD-Hoffy](https://github.com/HendryAvila/Hoofy) | Stage 3: Business Rules
>
> Business rules are the DNA of your system. They define what is acceptable
> and what is not — across ALL processes, not tied to any single feature.
> (Source: Business Rules Manifesto, Ronald Ross, v2.0)

## Definitions (Ubiquitous Language)

> Terms that everyone in the project must use consistently.
> (Source: Domain-Driven Design, Eric Evans — Ubiquitous Language)

Module: A self-contained learning unit covering a specific Go topic. Has an ID (1–12), title, subtitle, icon, duration, type, description, objectives, and sources.
ModuleProgress: Tracks a user's interaction with a module: completion status, score, maxScore, and timestamps (startedAt, completedAt).
Badge: A gamification reward earned upon completing a module. Has a unique ID, name, icon, description, and optional unlockedAt timestamp.
CourseState: The aggregate root of user progress — holds currentModule, all ModuleProgress records, totalScore, badges, dismissed vocabulary terms, userName, and startedAt.
Playground: An interactive code editor within a module that allows users to write and execute Go code.
Quiz: An assessment component within a module presenting multiple-choice or other question types.
BranchingScenario: A component presenting a decision tree with multiple outcomes based on user choices.
DragDrop: A component where users match or order items as a learning exercise.
Timer: A countdown component used in timed challenges within modules.
VocabularyFloat: A floating panel surfacing Go terminology relevant to the current module.
Certificate: A visual summary page showing the user's name, completion status, total score, and earned badges.

## Facts

> Relationships between terms that are always true.
> (Source: BRG Taxonomy — structural assertions)

FACT-001: The course has exactly 12 modules (IDs 1–12).
FACT-002: Each badge is associated with exactly one module completion event (except 'go-master' which requires all 12).
FACT-003: totalScore equals the sum of all individual module scores.
FACT-004: currentModule is always max(completedModuleId + 1, previous currentModule), capped at 13 (post-last module).
FACT-005: A module's startedAt timestamp is set once and never overwritten.
FACT-006: Progress is stored under the localStorage key 'go-course-progress'.

## Constraints

> Behavioral boundaries. Format: When <condition> Then <imposition> [Otherwise <consequence>]
> (Source: Business Rules Manifesto, BRG — action assertions)

BRC-001: When a user attempts to access module N, then the system must verify moduleId <= currentModule before rendering content.
BRC-002: When unlockBadge is called with a badgeId, then the badge must not already exist in the badges array (idempotent unlock).
BRC-003: When completeModule is called, then currentModule advances to min(moduleId + 1, 13) — never decrements.
BRC-004: When localStorage data is corrupted or missing, then the system must initialize a fresh CourseState.
BRC-005: When the user resets progress, then localStorage is cleared and CourseState is reinitialized to defaults.


## Derivations

> Knowledge computed or inferred from facts and constraints.
> (Source: BRG Taxonomy — derivations)

DRV-001: progressPercent = floor((completedModulesCount / 12) * 100).
DRV-002: A module is "unlocked" if its ID is less than or equal to currentModule.
DRV-003: The 'go-master' badge is derivable when all 12 modules are marked completed.


