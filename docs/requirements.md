> ⚡ Auto-generated by sdd_reverse_engineer — review and refine as needed

# go-course — Requirements

> Generated by [SDD-Hoffy](https://github.com/HendryAvila/Hoofy) | Stage 2: Specify

## Functional Requirements

### Must Have

FR-001: The system shall display 12 Go programming modules organized sequentially.
FR-002: The system shall unlock modules sequentially — module N+1 is only accessible after completing module N.
FR-003: The system shall track per-module progress (score, maxScore, startedAt, completedAt) and persist it in localStorage.
FR-004: The system shall award gamification badges when a module is completed.
FR-005: The system shall provide an interactive Quiz component for knowledge assessment within modules.
FR-006: The system shall provide a live Go Playground for in-browser code experimentation.
FR-007: The system shall provide a DragDrop activity component for hands-on exercises.
FR-008: The system shall provide a BranchingScenario component for decision-based learning.
FR-009: The system shall provide a Timer component for timed challenges.
FR-010: The system shall display a floating VocabularyFloat panel with Go terminology per module.
FR-011: The system shall allow the user to enter their name to personalize their experience and certificate.
FR-012: The system shall display a Results & Certificate page summarizing the user's progress and score.
FR-013: The system shall reset all progress on user request.
FR-014: The system shall deploy automatically as a static site to GitHub Pages on push to main.

### Should Have

FR-015: The system should show a progress bar indicating overall course completion percentage.
FR-016: The system should show a badge notification animation when a badge is unlocked.
FR-017: The system should show a SourcesSection with curated references per module.
FR-018: The system should provide a ModuleNav for in-module navigation (prev/next).

### Could Have

FR-019: Support for multiple users or user accounts.
FR-020: Server-side progress persistence (backend/database).
FR-021: Leaderboard comparing scores across users.
FR-022: Additional language support (English, Portuguese).

### Won't Have (this version)

No backend API. No user authentication. No server-side rendering. No external database.

## Non-Functional Requirements

NFR-001: The application must function as a fully static site (no server-side runtime required).
NFR-002: The application must be responsive across mobile, tablet, and desktop breakpoints.
NFR-003: All progress must survive page refreshes via localStorage — no data loss on reload.
NFR-004: The application must be deployable via GitHub Pages with a BASE_PATH of /go-course.
NFR-005: Build output must be a pre-rendered static bundle compatible with @sveltejs/adapter-static.
NFR-006: TypeScript strict mode must be enabled throughout the codebase.

## Constraints

TECH-001: Must use SvelteKit with @sveltejs/adapter-static (no server adapter).
TECH-002: Must use Svelte 5 runes API ($state, $derived, $effect) — not legacy stores where avoidable.
TECH-003: No backend or external API calls — all data is local.
TECH-004: Deployment target is GitHub Pages only.

## Assumptions

ASM-001: Users have a modern browser with localStorage support enabled.
ASM-002: The course is a single-user experience (no multi-user or auth).
ASM-003: Module content (quizzes, scenarios, exercises) is hardcoded in source — not fetched from a CMS.
ASM-004: The Go Playground connects to an external execution service (e.g., play.golang.org) or uses an embedded approach.

## Dependencies

_None identified._
